# OSATests

Unit-test target scaffold for app-shell, domain, persistence, retrieval, and policy tests.

## Notes

- Keep unit tests close to the narrow behavior being validated.
- Prefer deterministic tests over snapshot-heavy or UI-heavy coverage in this target.
