# Shared

Reusable UI and narrowly scoped cross-cutting helpers.

## Boundaries

- `DesignSystem/` holds design tokens and style primitives.
- `Components/` holds reusable view components that are not owned by a single feature.
- `Support/` holds focused non-visual helpers and shared support types.

Promote code here only when at least two app areas genuinely need it.
