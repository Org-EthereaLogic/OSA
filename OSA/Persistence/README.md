# Persistence

Local storage implementations and migration surfaces.

## Planned Subdomains

- `SwiftData/` — models, schemas, and repository implementations
- `SeedImport/` — bundled seed-content import and version checks
- `SearchIndex/` — sidecar search-index persistence and rebuild coordination
- `Migrations/` — schema and seed-content migration helpers
