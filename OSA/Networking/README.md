# Networking

Optional online enrichment surfaces that never bypass local persistence and attribution.

## Planned Subdomains

- `Clients/` — trusted-source search and fetch clients
- `ImportPipeline/` — normalization, trust checks, and local commit stages
- `Refresh/` — stale checks, retry coordination, and refresh orchestration
- `DTOs/` — network-facing transfer objects and parser inputs
