# Retrieval

Local evidence retrieval over handbook, quick cards, imported knowledge, and allowed user data.

## Planned Subdomains

- `Querying/` — query normalization and scope filtering
- `Ranking/` — deterministic scoring and result ordering
- `Chunking/` — citeable chunk production and stable local IDs
- `Citations/` — citation packaging for UI and assistant output
